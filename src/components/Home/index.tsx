"use client";
import { Box } from "@mui/material";

/**
 * The home page's main section. Intentionally empty — the notes canvas, board
 * preview and recent-posts preview that used to live here were removed.
 */
const Home: React.FC = () => (
  <Box sx={{ py: 2, px: { xs: 1, sm: 2, md: 3 }, width: "100%" }} />
);

export default Home;
